import { createFileRoute } from '@tanstack/react-router'
import { ArrowLeft, ArrowUpRight, Search, Sparkles, TrendingUp, Users } from 'lucide-react'
import { useMemo, useState } from 'react'
import { featuredPlayers, formatMoney } from '@/game-data'

export const Route = createFileRoute('/players')({
  head: () => ({ meta: [{ title: 'Jogadores · Futebol Diretor Pro' }, { name: 'description', content: 'Mercado de jogadores e avaliações de scout.' }] }),
  component: PlayersPage,
})

function PlayersPage() {
  const [query, setQuery] = useState('')
  const [league, setLeague] = useState('Todas')
  const filtered = useMemo(() => featuredPlayers.filter((player) => {
    const matchesQuery = `${player.name} ${player.club} ${player.nationality}`.toLowerCase().includes(query.toLowerCase())
    const matchesLeague = league === 'Todas' || (league === 'Brasil' ? ['Flamengo', 'Palmeiras', 'Fluminense', 'Wolverhampton'].includes(player.club) : true)
    return matchesQuery && matchesLeague
  }), [league, query])
  return <main className="min-h-dvh bg-background px-5 py-8 text-foreground md:px-10"><div className="mx-auto max-w-6xl"><a href="/" className="mb-8 inline-flex items-center gap-2 text-sm font-bold text-primary hover:underline"><ArrowLeft className="h-4 w-4" />Voltar ao centro de comando</a><div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.22em] text-primary">Mercado global</p><h1 className="mt-2 font-serif text-4xl">Jogadores & avaliações</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">Uma camada inicial de atletas de referência das seis ligas. Valores são estimativas de mercado, não cotações oficiais, e variam com forma, idade e contrato.</p></div><div className="flex items-center gap-2 rounded-lg border border-border bg-card px-4 py-3 text-xs"><Users className="h-4 w-4 text-primary" />{featuredPlayers.length} perfis curados</div></div><div className="mb-5 flex flex-col gap-3 sm:flex-row"><label className="flex flex-1 items-center gap-2 rounded-lg border border-border bg-card px-3 py-2.5"><Search className="h-4 w-4 text-muted-foreground" /><span className="sr-only">Buscar jogador</span><input value={query} onChange={(event) => setQuery(event.target.value)} className="w-full bg-transparent text-sm outline-none placeholder:text-muted-foreground" placeholder="Buscar por nome, clube ou nacionalidade" /></label><select value={league} onChange={(event) => setLeague(event.target.value)} className="rounded-lg border border-border bg-card px-3 py-2.5 text-sm outline-none"><option>Todas</option><option>Brasil</option></select></div><div className="overflow-hidden rounded-xl border border-border bg-card shadow-sm"><div className="hidden grid-cols-[1.7fr_1fr_0.7fr_0.8fr_1fr_0.8fr] gap-4 border-b border-border bg-muted/40 px-5 py-3 text-[10px] font-bold uppercase tracking-wider text-muted-foreground md:grid"><span>Atleta</span><span>Clube</span><span>Pos.</span><span>Idade</span><span>Valor estimado</span><span>Potencial</span></div>{filtered.map((player) => <div key={`${player.name}-${player.club}`} className="grid gap-3 border-b border-border px-5 py-4 transition-colors last:border-b-0 hover:bg-muted/30 md:grid-cols-[1.7fr_1fr_0.7fr_0.8fr_1fr_0.8fr] md:items-center md:gap-4"><div><div className="flex items-center gap-2"><p className="font-bold">{player.name}</p>{player.potential >= 93 && <Sparkles className="h-3.5 w-3.5 text-accent-foreground" />}</div><p className="mt-1 text-xs text-muted-foreground">{player.nationality} · contrato até {player.contract}</p>{player.note && <p className="mt-1 text-[10px] font-medium text-primary">{player.note}</p>}</div><p className="text-sm text-muted-foreground">{player.club}</p><p className="text-xs font-bold">{player.position}</p><p className="text-sm">{player.age}</p><p className="text-sm font-bold">{formatMoney(player.value, player.club === 'Palmeiras' || player.club === 'Flamengo' || player.club === 'Fluminense' ? 'BRL' : 'EUR')}</p><p className="flex items-center gap-1 text-sm font-bold text-primary">{player.potential}<TrendingUp className="h-3.5 w-3.5" /></p></div>)}{filtered.length === 0 && <div className="p-10 text-center text-sm text-muted-foreground">Nenhum jogador encontrado nessa busca.</div>}</div><p className="mt-4 flex items-center gap-2 text-xs text-muted-foreground"><ArrowUpRight className="h-3.5 w-3.5 text-primary" />Próxima camada: elencos completos por temporada, filtros por posição e negociação com parcelas.</p></div></main>
}
